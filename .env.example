# Security
JWT_SECRET_KEY="your-super-secret-key-at-least-32-chars-long"

# Database (SQLite default)
# No need to set DATABASE_URL for SQLite if using default config, 
# but helpful if you switch to Postgres later.
# DATABASE_URL="file:./dev.db"
